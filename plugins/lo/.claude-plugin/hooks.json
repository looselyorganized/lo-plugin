{
  "hooks": {
    "SessionStart": [
      {
        "type": "command",
        "command": "cat \"$(git rev-parse --show-toplevel 2>/dev/null || pwd)/.lo/project.yml\" 2>/dev/null || true"
      }
    ]
  }
}
